# Frontend Deployment

Staging is deployed with Azure Static Web Apps. The API base URL is injected during the GitHub Actions build via `VITE_API_BASE_URL_STAGING`.

Deploy trigger marker: 2026-05-26.
